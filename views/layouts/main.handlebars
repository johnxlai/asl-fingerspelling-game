<!DOCTYPE html>

 <head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>ASL Fingerspelling Game</title>

  {{!-- Style Reset --}}
  <link rel="stylesheet" type="text/css" href="/css/jade.css">

  {{!-- google font --}}
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Open+Sans:wght@300;400;700&family=Tilt+Warp&display=swap"
    rel="stylesheet">

  {{!-- Cdb tailwind for now comment out for prod --}}
  {{!-- <script src="https://cdn.tailwindcss.com"></script> --}}


  {{!-- Cdb tailwind for now in dev stage --}}
  <link rel="stylesheet" type="text/css" href="/css/style.css">

</head>

<body class="flex flex-col h-screen bg-neutral-800">
  <header class="flex flex-col md:flex-row justify-center items-center  md:justify-between p-8">
    <h1 class="text-slate-200 mb-4 md:mb-0">
      <a href="/"><img class="max-w-[100%] w-[180px]" src="/image/finger-spell-logo.svg" alt="The Finger Spell Game"></a>
    </h1>

    <nav class="text-slate-200 flex items-center gap-3">
      <a href="/start" class="text-purple-200">Play</a>
      <a href="/ranks" class="text-purple-200">Ranking</a>
      <a href="/practice" class="text-purple-200">Practice</a>

      {{#if loggedIn}}
        <a href="/profile" class="text-purple-200">Profile</a>
        <a href="/api/users/logout" class="text-purple-200">Logout</a>
      {{else}}
        <a href="/api/users/login" class="text-purple-200">Login</a>
        <a href="/api/users/create" class="text-purple-200">Signup</a>
      {{/if}}

      {{#if user.superuser }}
        <span class="text-sm px-2 bg-purple-500 font-semibold text-sm">SUPER</span>
      {{/if}}

    </nav>
  </header>

  <main class="px-5 py-2 grow flex justify-center items-center">
      {{{body}}}
  </main>

  <!-- If the user is logged in, load the JS script for when they logout -->

  <footer class="mt-auto p-5 text-center">
    <p class="text-white">Project by Yaro , John, Fabien</p>
  </footer>
</body>

</html>
